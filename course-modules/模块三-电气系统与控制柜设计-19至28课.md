# 模块三：电气系统与控制柜设计（第19—28课）

## 模块学习目标
掌握机器人整车电气架构设计，包括电源拓扑、电池BMS、急停安全回路、PLC/工控机分工、EMC抗干扰，能独立完成控制柜电气原理图绘制和施工。

---

## 第19课 整车多级电源拓扑结构
1.业务背景：机器人搭载多种电压等级设备（24V电机、12V工控机、5V传感器），电源拓扑设计不当会导致设备供电不稳、共地干扰。
2.底层原理：多级电源架构——电池总电压(48V) → 一级DC/DC(48→24V，驱动器+伺服) → 二级DC/DC(24→12V，工控机+雷达) → 三级LDO(12→5V，传感器+MCU)。关键指标：每级转换效率、纹波系数、瞬态响应、过流保护阈值。
3.硬件方案：48V/20Ah锂电池组 → 48V/24V/20A隔离DC/DC（金升阳/明纬） → 24V/12V/10A隔离DC/DC → 5V/3A LDO（AMS1117）。每级保险丝分级保护。
4.软件实现：电源状态监控ROS2节点——通过ADC采集各路电压电流，发布 /power_status 话题，低于阈值触发低电量保护策略。
```python
class PowerMonitor:
    """电源监控节点"""
    def __init__(self, thresholds={"48V": 42.0, "24V": 22.0, "12V": 10.5, "5V": 4.5}):
        self.thresholds = thresholds

    def check_voltage(self, readings: dict) -> list:
        """检测各路电压是否低于阈值，返回告警列表"""
        alerts = []
        for rail, voltage in readings.items():
            if rail in self.thresholds and voltage < self.thresholds[rail]:
                alerts.append(f"LOW VOLTAGE: {rail} = {voltage:.1f}V < {self.thresholds[rail]}V")
        return alerts
```
5.Demo：电源拓扑框图+各路电压监控Dashboard。
6.数据流：电池 → DC/DC各级转换 → 各设备供电 → ADC回采 → 监控节点 → 告警/保护。
7.调试手段：示波器测量各路纹波；电子负载模拟满载工况验证电源稳定性。
8.故障：DC/DC输出纹波过大导致工控机死机；根因：DC/DC滤波电容容量不足；修复：输出端并联470μF电解+0.1μF陶瓷电容。
9.工业案例：某AMR因5V传感器电源与12V共地，伺服启停瞬间5V跌落导致IMU数据丢帧，后将5V单独隔离供电解决。
10.面试题：机器人多级电源拓扑如何设计？各级DC/DC选型要点？

【入库产出】电源拓扑原理图+监控节点代码。

---

## 第20课 锂电池BMS保护系统原理
1.业务背景：锂电池过充过放会导致起火，BMS是安全底线，系统工程师必须理解其保护机制。
2.底层原理：BMS核心功能——(1)过充保护：单体>4.25V切断充电回路；(2)过放保护：单体<2.8V切断放电回路；(3)过流保护：电流>设定值断开；(4)温度保护：>60°C降功率，>80°C切断；(5)均衡：被动均衡（电阻放电）或主动均衡（电荷转移），使各单体电压一致。通信接口：SMBus/I2C/CAN上报电池状态。
3.硬件方案：本项目选用16S1P锂电池(48V/20Ah)，配成品BMS板（达锂/高特电子），支持CAN通信上报电压/电流/温度/SOC。
4.软件实现：ROS2 BMS驱动节点——通过CAN/串口读取BMS数据，发布 /battery_state 话题（std_msgs/BatteryState）。
```python
class BMSDriver:
    """BMS 数据读取"""
    def __init__(self, can_interface="can0"):
        self.interface = can_interface

    def parse_bms_frame(self, frame_data: bytes) -> dict:
        """解析 BMS CAN 帧"""
        return {
            "voltage_total": int.from_bytes(frame_data[0:2], 'big') * 0.01,  # V
            "current": int.from_bytes(frame_data[2:4], 'big', signed=True) * 0.1,  # A
            "soc": frame_data[4],  # %
            "temp_max": frame_data[5] - 40,  # °C
            "fault_code": frame_data[6],  # 故障码
        }
```
5.Demo：BMS数据读取+SOC曲线可视化+低电量告警脚本。
6.数据流：BMS ADC采样 → MCU计算 → CAN帧 → ROS2节点 → /battery_state → 任务调度器。
7.调试手段：CAN分析仪抓包BMS帧；模拟过放工况验证保护动作。
8.故障：BMS被动均衡效率低，某单体过放后整组不可用；根因：单体一致性差；修复：更换高一致性电芯或升级主动均衡BMS。
9.工业案例：某物流AMR因单体压差>200mV，BMS锁定不可充电，更换电芯后恢复。预防措施：出厂前做全容量分容匹配。
10.面试题：BMS的核心保护功能？被动均衡和主动均衡区别？

【入库产出】BMS选型文档+CAN驱动代码+保护策略文档。

---

## 第21课 续航指标下电池容量计算
1.业务背景：PRD中定义的续航时间需要精确计算电池容量，算小了跑不够时间，算大了增加成本和重量。
2.底层原理：容量计算公式：C = P_avg × T / η，其中 P_avg 为平均功耗(W)，T 为目标续航时间(h)，η 为放电效率(0.85~0.95)。平均功耗拆分：P = P_电机 + P_工控机 + P_传感器 + P_待机。注意低温环境容量衰减(-20°C时容量降至标称60%)。
3.硬件方案：本项目功耗估算——底盘驱动60W(巡航) + 工控机30W + 雷达15W + 相机5W + 其他10W = 120W。目标续航4h，C = 120×4/0.9 = 533Wh，48V系统需 533/48 = 11.1Ah，选配20Ah留余量。
4.软件实现：电池容量计算器脚本。
```python
def battery_capacity_calc(power_items: dict, target_hours=4.0, efficiency=0.9,
                          system_voltage=48.0, temp_de-rating=1.0):
    """电池容量计算"""
    P_total = sum(power_items.values())
    C_wh = P_total * target_hours / efficiency
    C_ah = C_wh / system_voltage
    C_actual = C_ah / temp_derating  # 低温衰减
    return {"total_power_W": P_total, "capacity_Wh": C_wh,
            "capacity_Ah": C_ah, "actual_Ah": C_actual}
```
5.Demo：续航计算Excel表+低温衰减曲线图。
6.数据流：各设备功耗 → 总功耗 → 容量计算 → 电芯选型 → BOM确认。
7.调试手段：样机实测功耗（接入功率计记录24h功耗曲线），对比计算值修正。
8.故障：计算功耗未包含机械臂峰值功耗，实际运行时电池提前耗尽；修复：补充峰值功耗计算并增大容量。
9.工业案例：某AMR标称续航8h，实测只有5h——根因是工控机CPU满载时功耗是标称的2倍。
10.面试题：如何计算机器人续航所需电池容量？低温对续航的影响？

【入库产出】电池容量计算书+功耗测试报告。

---

## 第22课 多路DC/DC隔离电源设计
1.业务背景：多路DC/DC需要电气隔离，避免大功率电机干扰敏感传感器供电。
2.底层原理：隔离DC/DC通过变压器实现输入输出电气隔离，隔离电压典型值1500VDC。非隔离DC/DC（Buck/Boost）成本低但无隔离，适合低敏感负载。选型参数：输入范围、输出电压/电流、效率、纹波、隔离电压、工作温度。
3.硬件方案：本项目电源方案——48V→24V/20A隔离模块(金升阳LH20-10B24)×2路并联 + 24V→12V/10A隔离模块 + 板载12V→5V/3A Buck( TI TPS5450 )。
4.软件实现：电源状态监控代码（见第19课），增加DC/DC输出故障检测逻辑。
5.Demo：电源原理图（含隔离DC/DC模块选型标注）。
6.数据流：电池48V → 隔离DC/DC → 各路输出 → 负载 → 回采检测。
7.调试手段：示波器测各路输出纹波（<100mVpp合格）；满载温升测试（<60°C）。
8.故障：隔离DC/DC输出振荡，纹波>500mV；根因：输出电容ESR偏高；修复：更换低ESR固态电容。
9.工业案例：某协作臂关节驱动器因非隔离DC/DC引入地环路，编码器噪声增大3倍，后改为隔离供电解决。
10.面试题：隔离DC/DC与非隔离DC/DC的适用场景？纹波如何抑制？

【入库产出】DC/DC选型表+电源原理图。

---

## 第23课 急停安全回路设计规范
1.业务背景：急停是机器人安全的最后防线，必须独立于软件，纯硬件实现，断电即停。
2.底层原理：安全急停回路遵循ISO 13850标准——(1)双通道冗余：急停按钮两组常闭触点串联，任一断开即动作；(2)安全继电器：PNOZ/安全PLC监测双通道状态，断开时切断所有动力回路；(3)正逻辑设计：断线=急停，避免线缆断裂时系统继续运行；(4)急停后必须手动复位，不允许自动恢复。
3.硬件方案：急停按钮（红色蘑菇头，带自锁）→ 安全继电器（Pilz PNOZ s4）→ 切断电机驱动器主电源+伺服使能信号。急停回路独立供电（24V电池直供），不受主电源开关影响。
4.软件实现：急停状态GPIO读取节点——检测急停信号后发布 /emergency_stop 话题，所有运动节点收到后立即停止发送指令。
```python
class EmergencyStopMonitor:
    """急停状态监控"""
    def __init__(self, gpio_pin=17):
        self.gpio_pin = gpio_pin
        self.estop_active = False

    def read_estop(self) -> bool:
        """读取急停GPIO状态，True=急停激活"""
        # 实际项目中使用 RPi.GPIO 或 sysfs 读取
        return self.estop_active

    def handle_estop(self, callback):
        """急停回调：立即停止所有运动指令"""
        if self.read_estop():
            callback("EMERGENCY_STOP: All motion halted")
```
5.Demo：急停回路原理图+安全继电器接线图+GPIO监控代码。
6.数据流：急停按钮 → 双通道触点 → 安全继电器 → 切断主电源+使能信号 → GPIO检测 → 软件停止。
7.调试手段：断开急停任一通道线缆，验证安全继电器是否动作；按急停按钮后测量驱动器端是否有残余电压。
8.故障：急停后驱动器仍有残余电压，电机有抖动——根因：急停只切了使能没切主电源；修复：急停回路同时切断主接触器和使能信号。
9.工业案例：某AGV急停只切断软件指令，未切断驱动器电源，急停后底盘因斜坡溜车，后增加硬件断电刹车。
10.面试题：急停回路的ISO 13850设计原则？为什么必须纯硬件实现？

【入库产出】急停安全回路原理图+安全继电器接线图。

---

## 第24课 安全继电器安全回路实现方案
1.业务背景：除急停外，还有防撞传感器、安全激光扫描仪、协作臂力反馈等安全信号，需统一接入安全回路。
2.底层原理：安全继电器模块分类——(1)紧急停止型(E-stop)：监测双通道急停按钮；(2)安全门型：监测防护门开关；(3)安全光幕型：监测光幕信号；(4)速度/位置监控型：监测运动是否超限。多个安全信号可串联形成安全链，任一触发即全系统停机。
3.硬件方案：本项目安全链——急停按钮 + 安全激光扫描仪(SICK microScan3,1个安全输出) + 协作臂安全IO → 安全继电器(Pilz PNOZ s4) → 切断动力回路。
4.软件实现：安全状态综合监控节点——汇总所有安全设备状态，统一发布 /safety_status。
5.Demo：安全链接线原理图+安全设备参数配置文档。
6.数据流：各安全传感器 → 安全继电器 → 动力切断 → 状态上报 → 上位机告警。
7.调试手段：逐一触发各安全设备，验证安全继电器动作和动力切断是否正常。
8.故障：安全激光扫描仪误触发频繁导致产线停机；根因：扫描仪灵敏度设置过高，将地面反光识别为障碍；修复：调整检测区域和灵敏度参数。
9.工业案例：某工厂协作臂+安全光幕组合，人员进入光幕范围即刻停机，离开后自动恢复，通过安全等级SIL2认证。
10.面试题：安全继电器的分类？多个安全信号如何组成安全链？

【入库产出】安全回路接线图+安全设备配置文档。

---

## 第25课 PLC与工控机软硬件分工边界划分
1.业务背景：机器人系统同时有PLC和工控机，必须明确各自职责边界，否则职责混乱导致重复开发和维护困难。
2.底层原理：分工原则——PLC负责：安全逻辑(急停/防护)、IO信号采集(限位/光电)、电机启停控制、运动互锁、周期<1ms的实时控制；工控机负责：导航算法、视觉感知、大模型推理、任务调度、人机界面、数据记录。接口：PLC与工控机通过Modbus TCP/OPC UA/Profinet交互状态和指令。
3.硬件方案：本项目——西门子S7-1200 PLC(安全逻辑+IO控制) + 工控机(i7+RTX3060,导航+视觉+AI) + Modbus TCP通信。
4.软件实现：PLC端用TIA Portal编写安全梯形图；工控机端用ROS2 pymodbus库读写PLC寄存器。
```python
from pymodbus.client import ModbusTcpClient

class PLCBridge:
    """工控机与PLC的Modbus TCP通信桥接"""
    def __init__(self, plc_ip="192.168.1.10", port=502):
        self.client = ModbusTcpClient(plc_ip, port=port)

    def read_estop_status(self) -> bool:
        """读取PLC急停状态寄存器"""
        result = self.client.read_holding_registers(address=100, count=1)
        return bool(result.registers[0] & 0x01)

    def write_motion_enable(self, enable: bool):
        """写入运动使能寄存器"""
        value = 1 if enable else 0
        self.client.write_register(address=200, value=value)
```
5.Demo：PLC梯形图程序+ROS2 Modbus桥接节点+联合调试记录。
6.数据流：传感器→PLC(IO采集+安全判断) ↔ Modbus TCP ↔ 工控机(导航+AI决策) → PLC(运动指令) → 电机。
7.调试手段：Modbus Poll工具模拟PLC通信；TIA Portal在线监控PLC状态。
8.故障：PLC与工控机通信断开后电机仍按最后指令运行——根因：未实现心跳超时保护；修复：PLC侧增加心跳监测，超时500ms自动停机。
9.工业案例：某AGV将急停逻辑放工控机，工控机死机后急停失效，后全部迁移至PLC独立回路。
10.面试题：PLC与工控机如何划分职责边界？通信断开后的安全策略？

【入库产出】PLC-工控机分工文档+Modbus通信代码+PLC梯形图。

---

## 第26课 IO继电器、接触器控制系统设计
1.业务背景：机器人需要控制多种外设（灯、喇叭、电磁阀、充电对接），需要IO继电器和接触器驱动。
2.底层原理：继电器：小信号控制大负载，隔离控制侧与负载侧；接触器：大电流开关（>10A），用于电机主回路通断；固态继电器(SSR)：无机械触点，开关速度快，寿命长但散热要求高。IO模块输出类型：继电器输出(2A)vs晶体管输出(0.5A,可PWM)。
3.硬件方案：本项目IO方案——PLC扩展8路继电器输出模块(控制灯/喇叭/电磁阀) + 1个主接触器(控制48V动力回路) + 固态继电器(控制加热器)。
4.软件实现：ROS2 IO控制节点——通过PLC Modbus写入输出寄存器控制继电器。
5.Demo：IO分配表+继电器接线图+控制节点代码。
6.数据流：ROS2指令 → Modbus写寄存器 → PLC输出 → 继电器/接触器 → 外设。
7.调试手段：万用表测量继电器线圈电压和触点通断；长时间带载测试接触器温升。
8.故障：继电器触点粘连，断电后设备仍运行；根因：驱动感性负载(电磁阀)未加续流二极管，触点烧蚀；修复：线圈侧并联续流二极管1N4007。
9.工业案例：某AMR充电对接机构使用继电器控制电磁阀，因未加续流二极管，6个月后触点粘连，充电不可控。
10.面试题：继电器与接触器的区别？驱动感性负载为什么需要续流二极管？

【入库产出】IO分配表+继电器接线原理图。

---

## 第27课 EMC接地、布线、抗干扰方案
1.业务背景：机器人内部伺服驱动器、开关电源、电机是强干扰源，必须做好EMC设计，否则传感器数据噪声大、通信丢包。
2.底层原理：EMC三要素——干扰源(电机/驱动器)、耦合路径(传导/辐射)、敏感设备(编码器/IMU/通信线)。抑制方法：(1)接地：机壳单点接地，信号地与功率地分开；(2)布线：强弱电分离走线，间距>50mm，交叉90°；(3)屏蔽：信号线双绞+铝箔屏蔽，驱动器输出线用屏蔽线，屏蔽层单端接地；(4)滤波：电源入口加共模电感+X/Y电容(EMI滤波器)，驱动器输出加du/dt滤波器。
3.硬件方案：本项目EMC方案——机壳接地电阻<0.1Ω，CAN/编码器线双绞屏蔽，伺服驱动器输出加磁环，电源入口EMI滤波器，控制柜强弱电分区布线。
4.软件实现：通信丢包率监控节点——统计CAN/以太网丢包率，超过阈值告警。
```python
class EMCMonitor:
    """EMC干扰监控"""
    def __init__(self, packet_loss_threshold=0.01):
        self.threshold = packet_loss_threshold
        self.stats = {}

    def update(self, interface: str, sent: int, received: int):
        loss_rate = 1.0 - received / max(sent, 1)
        self.stats[interface] = {"sent": sent, "received": received, "loss_rate": loss_rate}
        if loss_rate > self.threshold:
            return f"EMC ALERT: {interface} packet loss {loss_rate:.2%} > threshold"
        return None
```
5.Demo：EMC布线规范文档+接地示意图+丢包监控脚本。
6.数据流：干扰源 → 耦合路径 → 敏感设备 → 数据异常检测 → 告警。
7.调试手段：频谱仪测辐射干扰；近场探头定位干扰源；CAN总线示波器看波形畸变。
8.故障：CAN总线间歇性丢帧——根因：伺服驱动器线与CAN线平行走线30cm，驱动器PWM谐波耦合到CAN线；修复：CAN线改走线槽并加磁环。
9.工业案例：某AGV在充电站附近IMU数据跳变严重，根因为充电机EMI辐射超标，后增加充电机屏蔽罩和滤波器。
10.面试题：EMC三要素？机器人内部强弱电布线规范？

【入库产出】EMC布线规范文档+接地示意图+干扰监控代码。

---

## 第28课 机器人控制柜布局、散热、布线工艺
1.业务背景：控制柜是机器人电气核心，布局不合理会导致散热差、维护难、布线混乱。
2.底层原理：控制柜设计原则——(1)分区：强电区(驱动器/接触器/断路器)在上，弱电区(PLC/工控机/IO模块)在下，中间用金属隔板屏蔽；(2)散热：发热元件(驱动器/工控机)靠近进风口，出风口在顶部(热空气上浮)，计算散热面积Q=P/(k·ΔT)；(3)线缆管理：强弱电分线槽，走线横平竖直，标签编号统一；(4)维护：可抽拉导轨安装，接线端子排统一朝下，预留30%空间。
3.硬件方案：本项目控制柜——400×300×200mm铝合金机箱，顶部风扇+侧面进风口，底部出线格兰头，内部导轨安装PLC+DC/DC+端子排+工控机。
4.软件实现：控制柜温度监控——工控机主板温度传感器+外部NTC，发布 /cabinet_temp 话题。
5.Demo：控制柜3D布局图+散热计算Excel+布线规范文档。
6.数据流：各发热设备功耗 → 散热面积计算 → 风扇选型 → 温度监控 → 过热降频/停机。
7.调试手段：热成像仪扫描控制柜内部热点；满载运行4小时测温升曲线。
8.故障：工控机在夏天频繁降频死机——根因：控制柜散热面积不足，内部温度>70°C；修复：增加强制风冷风扇和进风口面积。
9.工业案例：某AMR控制柜未做强弱电分区，驱动器干扰导致工控机网口丢包10%，重新分区布线后丢包降至0.01%。
10.面试题：控制柜强弱电分区原则？散热面积如何计算？

【入库产出】控制柜布局图+散热计算书+布线规范文档。

---

### 逆向案例③：商用机器人控制柜硬件逆向分析
拆解电源链路、保险丝分级、硬件急停回路、伺服驱动单元、PLC与IO模块，输出电气原理图。

【模块三入库文件：电源拓扑图、BMS文档、安全回路图、控制柜布局图、EMC规范、电气原理图】
